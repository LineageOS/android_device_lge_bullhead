allow bluetooth sysfs:file rw_file_perms;

allow bluetooth ttyHS0_device:chr_file { open ioctl rw_file_perms };

allow bluetooth start_hci_filter:unix_stream_socket connectto;

#HACK allow bluetooth default_prop:property_service set;
